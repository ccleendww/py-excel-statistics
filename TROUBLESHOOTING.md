## Excel 数据获取故障诊断与修复

### 问题描述
获取 Excel 中选中的数据失败，自动刷新和主动刷新都无法正常工作。

### 根本原因
**COM（Component Object Model）多线程初始化问题**  
在后台监听线程中调用 `win32com.client.GetActiveObject()` 时，需要先初始化 COM（`pythoncom.CoInitialize()`）。否则会抛出错误：
```
-2147221008: '尚未调用 CoInitialize。'
```

### 修复内容

#### 1️⃣ 新增日志模块 (`logger.py`)
- 记录所有应用日志到 `logs/` 目录
- 同时输出到控制台便于调试
- 提供 `log_info()`、`log_error()` 等方法

#### 2️⃣ 新增 Excel 连接器 (`excel_connector.py`)
- 所有 Excel 操作集中在 `ExcelConnector` 类
- 每次调用 `get_selected_cells()` 前都会 **自动初始化 COM**
- 支持重试机制（最多 3 次）
- 完整的错误诊断和状态检查
- 提供 `diagnose()` 方法进行连接状态诊断

#### 3️⃣ 改进主应用 (`main.py`)
- 使用 `ExcelConnector` 替代直接调用 `win32com`
- 改进 `monitor_excel_selection()` 线程错误处理
- 在 UI 中显示详细的诊断信息（当无法获取数据时）
- 添加错误计数和友好的错误提示

### 使用指南

#### 快速测试 Excel 连接
```bash
python diagnose.py
```

**输出示例（成功）：**
```
============================================================
Excel 连接诊断工具
============================================================

正在诊断 Excel 连接状态...

✓ Excel 应用已运行
✓ 活跃工作簿: CD8086-IR28DAYS.xlsx
✓ 选择: B5 (123)

------------------------------------------------------------
尝试获取选中的单元格...

✓ 成功获取 1 个单元格:

  - B5: 123

============================================================
```

#### 启动主应用
```bash
python main.py
```

#### 查看日志
```bash
cat logs/app_*.log
```

### 故障排除

如果仍无法获取数据，请检查以下几点：

| 检查项 | 解决方案 |
|------|--------|
| **Excel 未打开** | 打开 Excel 应用 |
| **没有选中单元格** | 在 Excel 中选择至少一个单元格 |
| **COM 初始化失败** | 重启应用或计算机；检查 Python 版本兼容性 |
| **防火墙/杀软阻止** | 将 Python 添加到防火墙白名单 |
| **权限不足** | 以管理员身份运行 Python |
| **驱动器网络映射问题** | 使用本地驱动器，避免网络路径 |

### 技术细节

#### COM 初始化修复
```python
def _init_com(self):
    """在多线程环境中必须初始化 COM"""
    try:
        pythoncom.CoInitialize()
    except Exception as e:
        log_debug(f"COM 初始化: {e}")
```

每次调用 Excel API 前都会执行此方法，确保在后台线程中也能正确调用 COM 对象。

#### 错误处理改进
- **返回值**：`(cells_data, error_msg)` 元组，便于区分成功/失败
- **诊断信息**：无数据时显示详细的 Excel 连接状态
- **日志记录**：所有操作都会被记录，便于事后分析

### 新增文件

```
py_excel/
├── logger.py              # 日志模块（新增）
├── excel_connector.py     # Excel 连接器（新增）
├── diagnose.py            # 诊断工具（新增）
├── logs/                  # 日志目录（自动创建）
│   └── app_20260209.log
└── ...
```

### 验证步骤

1. **确保 Excel 已打开且有数据被选中**
2. **运行诊断工具验证连接**
   ```bash
   python diagnose.py
   ```
3. **启动主应用**
   ```bash
   python main.py
   ```
4. **在 Excel 中改变选择，观察 UI 更新**
   - 应该能看到"✓ 已检测到选中的单元格"
   - 上方的文本区应显示选中的单元格信息

### 常见问题

**Q: 应用启动后仍显示"✗ 无法连接到 Excel"**  
A: 
1. 确认 Excel 已打开
2. 运行 `python diagnose.py` 检查具体错误
3. 查看 `logs/app_*.log` 了解详细错误信息
4. 尝试重启 Excel 和应用

**Q: 日志显示乱码**  
A: 这是控制台编码问题，不影响功能。查看 `logs/` 目录下的 `.log` 文件可以看到正确的中文。

**Q: "获取数据"按钮仍然不工作**  
A: 
1. 确认自动刷新已成功获取数据（观察 UI 显示）
2. 手动点击"刷新"按钮尝试手动触发
3. 如果仍不行，查看日志里 `fetch_data()` 的错误信息

### 扩展功能

诊断工具可以集成到 UI 中：
- 添加"诊断"按钮，实时显示连接状态
- 在状态标签显示更详细的错误信息
- 自动重连机制

